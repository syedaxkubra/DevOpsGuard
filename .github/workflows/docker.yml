name: Docker Build

on:
  push:
    branches:
      - master

jobs:
  docker-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build backend Docker image
        run: docker build -t devopsguard-backend ./backend

      - name: Build frontend Docker image
        run: docker build -t devopsguard-frontend ./frontend